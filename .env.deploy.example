# 部署配置：只在部署 WalletRegistry / SmartWallet 时用到

# WalletRegistry owner（pause / admin 白名单 / gasReceive 白名单控制权）
REGISTRY_OWNER=

# 部署后立即加白名单，可选，逗号分隔多个地址
ADMIN_ADDRESSES=
GAS_RECEIVE_ADDRESSES=

# 可选，覆盖默认 3000000 的部署 gas limit
DEPLOY_GAS_LIMIT=

# 合约开源验证（Etherscan V2 API）
ETHERSCAN_API_KEY=
