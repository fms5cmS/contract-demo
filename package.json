{
  "name": "contract-self",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "mise exec -- hardhat compile",
    "test": "mise exec -- hardhat test",
    "clean": "mise exec -- hardhat clean",
    "node": "mise exec -- hardhat node",
    "deploy:smart-wallet": "mise exec -- hardhat run scripts/deploy-smart-wallet.js --network custom",
    "swap": "node scripts/swap-via-okx.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "6.1.2",
    "@openzeppelin/contracts": "5.4.0",
    "dotenv": "17.3.1",
    "hardhat": "2.28.6",
    "viem": "^2.21.0"
  }
}
